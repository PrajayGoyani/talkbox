export enum Route {
  LOGIN = "/login",
  SIGNUP = "/signup",
  CHAT = "/chat",
  CONVERSATIONS = "/chat/conversations",
  PROFILE = "/chat/profile",
  SETTINGS = "/chat/settings",
  REQUESTS = "/chat/requests",
  TERMS = "/terms",
  PRIVACY = "/privacy",
  PRICING = "/pricing",
  FAQ = "/faq",
}
