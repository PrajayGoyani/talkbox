{
  "configurations": [
    {
      "name": "Attach to process",
      "type": "bun",
      "request": "attach",
      "url": "ws://localhost:9229/talkbox",
      "localRoot": "${workspaceFolder}",
      "remoteRoot": "/src"
    }
  ]
}
